{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "debug:server",
      "type": "shell",
      "command": "npm run debug:server",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "debug:client",
      "type": "shell",
      "command": "npm run debug:client",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
